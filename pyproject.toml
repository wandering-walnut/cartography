[build-system]
requires = [
  "setuptools",
  "setuptools-scm",
  "wheel"
]
build-backend = "setuptools.build_meta"

[tool.setuptools_scm]
# See configuration details in https://github.com/pypa/setuptools_scm
write_to = "cartography/_version.py"
fallback_version = "0.0.0"

[project]
name = "cartography"
description = "Explore assets and their relationships across your technical infrastructure."
readme = "README.md"
license = "Apache-2.0"
license-files = ["LICENSE"]
maintainers = [
    { name = "Cartography Contributors" }
]
classifiers = [
  'Development Status :: 4 - Beta',
  'Intended Audience :: Developers',
  'Natural Language :: English',
  'Programming Language :: Python',
  'Programming Language :: Python :: 3',
  'Programming Language :: Python :: 3.11',
  'Programming Language :: Python :: 3.12',
  'Programming Language :: Python :: 3.13',
  'Topic :: Security',
  'Topic :: Software Development :: Libraries',
  'Topic :: Software Development :: Libraries :: Python Modules',
]
requires-python = ">=3.11"
# By convention (per pip/uv recommendations), dependencies should use a loose
# lower bound and no upper bound unless explicitly needed. Exact pinning is
# handled by the lockfile (uv.lock).
dependencies = [
    "backoff>=2.1.2",
    "boto3>=1.15.1",
    "aioboto3>=15.0.0",
    "httpx>=0.24.0",
    "botocore>=1.18.1",
    "dnspython>=2.0.0",
    "neo4j>=6.0.0",
    "policyuniverse>=1.1.0.0",
    "google-api-python-client>=2.0.0",
    "google-auth>=2.37.0",
    "google-cloud-storage>=2.0.0",
    "marshmallow>=4.0.0",
    "oci>=2.71.0",
    "okta>=3.4.4",
    "PyJWT[crypto]>=2.0.0",
    "cryptography>=45.0.0",
    "pyyaml>=5.3.1",
    "requests>=2.30.0",
    "urllib3>=2.0.0",
    "statsd>=4.0.0",
    "packaging>=26.0.0",
    "packageurl-python>=0.17.0",
    "azure-mgmt-compute>=38.1.0",
    "azure-mgmt-containerinstance>=10.0.0",
    "azure-mgmt-keyvault>=10.0.0",
    "azure-mgmt-resource>=26.0.0,<27",
    # Split out of azure-mgmt-resource in 25.0.0; provides azure.mgmt.resource.subscriptions.
    "azure-mgmt-resource-subscriptions>=1.0.0,<2",
    "azure-mgmt-cosmosdb>=10.0.0",
    "azure-mgmt-web>=11.0.0",
    "azure-mgmt-eventgrid>=10.0.0",
    "azure-mgmt-logic>=10.0.0",
    "azure-mgmt-eventhub>=10.1.0",
    "azure-keyvault-secrets>=4.0.0",
    "azure-keyvault-keys>=4.0.0",
    "azure-keyvault-certificates>=4.0.0",
    "azure-mgmt-datafactory>=10.0.0",
    "azure-mgmt-containerservice>=41.0.0",
    "azure-mgmt-network>=31.0.0",
    "azure-mgmt-security>=5.0.0",
    "azure-mgmt-monitor>=3.0.0",
    "azure-mgmt-managementgroups>=2.0.0",
    "msrestazure>=0.6.4",
    "azure-mgmt-storage>=25.0.0",
    "azure-mgmt-sql>=4.0.0",
    "azure-mgmt-authorization>=0.60.0",
    "azure-identity>=1.5.0",
    "azure-storage-blob>=12.0.0",
    "msgraph-sdk>=1.53.0",
    "kubernetes>=22.6.0",
    "pagerduty>=4.0.1",
    "crowdstrike-falconpy>=0.5.1",
    "python-dateutil>=2.9.0",
    "xmltodict>=1.0.0",
    "duo-client>=5.5.0",
    "cloudflare>=4.1.0",
    "scaleway>=2.10.0",
    "google-cloud-resource-manager>=1.14.2",
    "google-cloud-asset>=1.0.0",
    "google-cloud-artifact-registry>=1.21.0",
    "google-cloud-aiplatform>=1.127.0",
    "google-cloud-run>=0.13.0",
    "types-aiobotocore-ecr>=3.1.0",
    "typer>=0.9.0",
    "azure-mgmt-synapse>=2.0.0",
    "azure-synapse-artifacts>=0.17.0",
    "slack-sdk>=3.37.0",
    "workos>=8.0.0",
    # Modal ships no public REST API, so the intel module talks to the gRPC service
    # through this client. Upper-bounded because the module also uses Modal's internal
    # protocol (see cartography/intel/modal/util.py), which is unversioned.
    #
    # The 1.5.3 floor is not cosmetic: the EnvironmentGetRoles RPC the module needs for
    # per-environment RBAC does not exist in 1.5.0-1.5.2, and the resulting AttributeError
    # is not a Modal API error, so it would abort the whole module rather than skip a
    # resource. Every other RPC used is present from 1.5.0.
    "modal>=1.5.3,<2",
    "pydo>=0.40.0",
]
# Comes from Git tag
dynamic = [ "version" ]

[project.optional-dependencies]
# Swaps the pure-Python Bolt codec for the Rust one maintained by Neo4j. It ships as a
# separate distribution that installs itself as `neo4j._rust`, which the driver picks up
# on its own, so nothing in cartography imports it and nothing needs to be configured.
# Install with `cartography[neo4j-rust]` to get it; plain `cartography` is unchanged.
neo4j-rust = [
    "neo4j-rust-ext>=6.0.0.0",
]

[dependency-groups]
dev = [
    "moto>=5.0.0",
    "pre-commit>=4.0.0",
    "pytest>=6.2.4",
    "pytest-mock>=3.15.0",
    "pytest-cov>=7.0.0",
    "pytest-rerunfailures>=16.0.0",
    "pytest-asyncio>=1.0.0",
    "testcontainers>=4.14.0",
    "types-PyYAML>=6.0.0",
    "black>=26.0.0",
    "types-requests>=2.32.0",
]
doc = [
    "myst-parser[linkify]>=4.0.1",
    "shibuya>=2025.4.25",
    "sphinx>=8.1.3",
    "sphinx-autobuild>=2024.10.3",
    "sphinx-copybutton>=0.5.2",
    "sphinx-sitemap>=2.8.0",
    "sphinxcontrib-mermaid>=1.0.0",
]

# Makes sure to look inside cartography/cartography/ for cli command
[tool.setuptools.packages.find]
where = ["."]
include = ["cartography*"]

[project.scripts]
cartography = "cartography.cli:main"
cartography-detectdrift = "cartography.driftdetect.cli:main"
cartography-rules = "cartography.rules.cli:main"

[tool.setuptools.package-data]
"cartography" = ["py.typed"]
"cartography.data" = ["*.cypher", "*.yaml"]
"cartography.data.jobs.analysis" = ["*.json"]
"cartography.data.jobs.scoped_analysis" = ["*.json"]
"cartography.data.jobs.cleanup" = ["*.json"]

[project.urls]
Homepage = "https://docs.cartography.dev"
Documentation = "https://docs.cartography.dev"
Repository = "https://github.com/cartography-cncf/cartography"
Issues = "https://github.com/cartography-cncf/cartography/issues"
Changelog = "https://github.com/cartography-cncf/cartography/releases"
